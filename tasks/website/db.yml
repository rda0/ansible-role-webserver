- name: include db instance
  include_tasks: website/db_instance.yml
  vars:
    _databases: "{{ _webserver_vhosts | selectattr('website.db', 'equalto', True) | selectattr('name', 'in', _webserver_vhostfilter) | list | map(attribute='db') | list }}"
