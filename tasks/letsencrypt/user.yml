- name: create local letsencrypt user
  user:
    name: letsencrypt
    comment: LetsEncrypt
    home: '{{ webserver_letsencrypt_path }}'
    create_home: no
    shell: /bin/bash
    system: yes
    group: ssl-cert
    groups: ssl-cert
