[Init]

block = \/[^,]*(\.php)[^,]*

[Definition]

datepattern = {^LN-BEG}

prefregex =

failregex = ^[^:]+:[^ ]+ <HOST> .* \"(?:GET|POST|HEAD) <block> HTTP\/\d+(?:\.\d+)?\" (?:4|5)\d{2} .*$

ignoreregex =
